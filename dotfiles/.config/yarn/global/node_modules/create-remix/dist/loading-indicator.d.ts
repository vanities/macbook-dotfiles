/// <reference types="node" />
export declare function renderLoadingIndicator({ start, end, while: update, noMotion, stdin, stdout, }: {
    start: string;
    end: string;
    while: (...args: any) => Promise<any>;
    noMotion?: boolean;
    stdin?: NodeJS.ReadStream & {
        fd: 0;
    };
    stdout?: NodeJS.WriteStream & {
        fd: 1;
    };
}): Promise<void>;
